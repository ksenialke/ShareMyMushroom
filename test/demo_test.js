// Describe tests
const assert = require('assert');

describe('some demo test', function () {

    // Create tests
    it('adds two numbers together', function () {
        assert( 2 + 3 === 5 );
    })

});